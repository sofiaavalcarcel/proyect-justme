services:
  api:
    build: .
    container_name: justme_api_stg
    env_file:
      - .stg.env
    ports:
      - "3001:3000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - stg_net

  postgres:
    image: postgis/postgis:17-3.5
    container_name: justme_postgres_stg
    environment:
      POSTGRES_DB: justme_stg_db
      POSTGRES_USER: stg_user
      POSTGRES_PASSWORD: 123456
    ports:
      - "5433:5432"
    volumes:
      - postgres_stg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stg_user -d justme_stg_db"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - stg_net

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin_stg
    environment:
      PGADMIN_DEFAULT_EMAIL: root@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    ports:
      - "5051:80"
    networks:
      - stg_net

volumes:
  postgres_stg_data:

networks:
  stg_net:
